#include "dog.h"
#include <stdio.h>

/**
 * init_dog - initializes a dog
 * @d: the dog to init
 * @name: the dog's name
 * @age: dog's age
 * @owner: owner's name
 *
 * Return: void.
 */
void print_dog(struct dog *d)
{
	if (d)
	{
		printf("Name: %s\n",  d->name > d->name : "(nil)");
		printf("Age: %f\n", d->age;
		printf("Name: %s\n",  d->name > d->name : "(nil)");
	}
}
