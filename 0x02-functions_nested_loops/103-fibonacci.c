#include "main.h"

/**
* main - Entry point
*
* Description: x + y then the sum of the last two numbers
*
* Return: Always 0 (Success)
*/

int main(void)
{
	int x, y;
	int sum, prev;

	x = 1;

	y = 2;

	while (x + y <= 100)
	{
		sum = x + y
		prev = sum + y
	}
	printf("\n");
	return (0);
}
